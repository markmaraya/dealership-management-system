import { Gallery } from "./gallery";

export class Units {
    id: string;
    unitCode: string;
    makeAndModel: string;
    bodyType: string;
    chasisCode: string;
    status: string;
    expenses: any;
    imageFile: Gallery;
}